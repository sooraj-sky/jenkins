# Use the official Ubuntu as the base image
FROM ubuntu:20.04

# Set environment variables to avoid interaction during installation
ENV DEBIAN_FRONTEND=noninteractive

# Update the package list and install the PostgreSQL 16 client
RUN apt-get update && apt-get install -y postgresql-client-16

# Clean up apt cache to reduce the image size
RUN apt-get clean && rm -rf /var/lib/apt/lists/*

# Define a default command to run when the container starts
CMD ["psql", "--version"]
I apologize for missing the recent training session. It was my oversight, and I take full responsibility. I understand the importance of these sessions.

To prevent this in the future, I will check for upcoming trainings twice a week. I appreciate your understanding.
